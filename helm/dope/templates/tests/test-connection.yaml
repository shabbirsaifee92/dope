apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dope.fullname" . }}-test-connection"
  labels:
    {{- include "dope.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dope.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
